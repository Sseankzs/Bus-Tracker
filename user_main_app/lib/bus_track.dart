import 'package:flutter/material.dart';
import 'package:user/map.dart';

class BusTrack extends StatelessWidget {
  const BusTrack({super.key});

  @override
  Widget build(BuildContext context) {
    MapPage mp1 = const MapPage();
    return mp1;
  }
}
